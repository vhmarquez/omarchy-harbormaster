# M1 completion and current handoff

M1 is complete. Owner-approved PRs #49–#52 are merged; issues #7–#10 and
milestone 2 are closed and read back. The overall roadmap #1 remains open.
Final M1 main `9e8b2d2d45b2f6ff7d574c339c90616ef4cfc16a` passed fresh post-merge
Docker portable 32, local combined 34 and separate native 5 required checks.
Actual paired qualification passed for 289 selected files. No required skips;
only the three declared optional live/future probes skipped.
[Final main evidence](https://github.com/vhmarquez/omarchy-harbormaster/issues/10#issuecomment-5562630253)
records hosted run 34063814069 and inspected artifact 9998370297.

The library includes typed private IPC/admission, the bounded SQLite worker,
pure reducers, durable event coordination, bounded recovery artifacts and
read-only diagnostics. Production adapter eligibility remains empty. These
results do not claim a deployed manager, native Hermes turns or achieved runtime
performance budgets. Historical receipts remain in `evidence/m1-7` through
`evidence/m1-10`; they are not overwritten by later summaries.

The owner has authorized continued development toward a working Hermes plugin
under the revised roadmap and a proportionate testing policy. PR #53 is approved
and merged; #11 is complete at qualified main `59880ff`. The #12 candidate adds
the runnable manager and independent terminal runtime. See
[M2 #12](M2-12-RUNTIME.md) and [CLI usage](../crates/harbormaster/README.md).
Its PR/revision needs explicit owner approval before merge or dependent #13
implementation. M2/M3 delivery remains open; no desktop deployment, live profile
activation or credentialed/model-driven test is implied.

## Preserved boundaries

Keep Docker locked down and native bubblewrap separately mandatory, including
both original M0 methods and the unmapped peer PID method. Pair actual trusted
receipts against the exact selected source bytes; evidence consistency is not
execution attestation or protection against hostile same-UID mutation.
Only standard free public-repository CI is authorized. Dedicated issue branches,
separate editing worktrees, pinned private tools and no direct-main feature
commits remain required. Worktrees are not security sandboxes.

The approved history policy remains 7/30 days, default 30, with 20,000 facts.
Tombstones separately retain at most 20,000/30 days; eviction retires affected
generations and requires reconciliation. Active/unreviewed obligations remain
protected, with separate 1,000 pending/7-day outbox expiry. MIT, frozen designs,
native harness approvals, Codex Limited visibility, live Watcher, real sessions,
credentials, profiles, configuration and historical tool/report roots remain.
