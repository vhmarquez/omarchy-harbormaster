# M1 #10 evidence index

This candidate depends on the owner-approved PR #51 merge. The actual main
[post-merge summary](post-merge-main/summary.json) records hosted Docker portable
28 required PASS, combined 30 PASS, native 5 PASS and actual paired qualification
for 215 selected files at 23be30f0d8fa4fb9767241d08faa0a543a3c950b.
Issue #9 is closed. Issue #10 and M1 remain open pending this candidate's review,
exact-head Docker/native qualification, explicit owner approval, merge and fresh
main qualification. M2 has not begun.

## Executed development evidence

- [Pure domain](domain/): original behavioral RED/GREEN and the first independent
  ordering corrections. [Reconciliation](domain-reconciliation/) adds fresh
  identity/current-turn comparisons and the independently reproduced process-loss,
  unknown-terminal and explicit-freshness corrections. The corrected domain suite
  has 33 passing cases; those receipts identify their tested selected bytes.
- [Storage](storage/): behavioral RED/GREEN for V3 scoped effects, exact write
  identity, independent obligation resolution/review/delivery, policy and bounded
  cleanup plans; real SQLite ENOSPC; verified wait continuity; atomic discarded
  count overflow. Historical intermediate failures remain explicitly retained.
- [Ingestion](ingestion-install/): actual private Unix peers prove exact committed
  generation installation, invalidation and preserved admission bounds.
- [Recovery artifacts](recovery-artifacts/): no production mapping is eligible.
  Private synthetic reviewed/mis-mapped sources exercise content canaries,
  physical spool/log bounds, descriptor/inode/owner locks, sequence-before-batch
  ordering, orphan cleanup, actual SIGKILL and private-tmpfs ENOSPC.
- [Coordinator](coordinator/): compiled behavioral RED 4 and initial GREEN 4, then
  expanded 11-case execution with real peers, exact duplicate receipt recovery,
  pending/backpressure, explicit revision conflict, deliberate loss of an actual
  worker reply, and SIGKILL after SQLite commit before spool unlink. A compile
  setup failure and initial strict-Clippy failures are preserved separately from
  behavioral RED; the corrected strict whole-workspace Clippy passed.
- [Diagnostics](diagnostics/): mismatched revision RED, immutable snapshot GREEN 4,
  100 serializations preserving actual state bytes/modes/mtimes/policy, maximum
  numeric width size bound, and actual causal guard/privacy failures. One stale
  Cargo cache restoration failure is explicitly recorded and corrected using a
  fresh source snapshot and isolated package clean; it is not product RED.
- [Verifier inventory](verification-inventory/): observed missing-target RED 8,
  original exact-count assertions failing after inventory growth, corrected
  121-case GREEN, and actual fixed-filesystem mount/security-policy RED/GREEN.
  The required native inventory was not changed or made optional.

## Review and qualification boundaries

[Independent recovery review](independent-recovery/review-final.json) passed the
exact recovery source at 0e7589c. It independently reproduced retained unlinked
log bytes exceeding 4 MiB, then reran that unchanged regression with zero retained
regular-file descriptors. Its actual compiled causal mutations fail; source-bound
logs/fixtures and the accepted cohesive 52-line stage durability unit are retained.
The [domain/storage review](independent-domain-storage/) records additional
causal failures and corrections; final integrated disposition remains required.

Component counts are not summed into a final qualification claim. Evidence
consistency is not cryptographic attestation or same-UID tamper protection.
Canonical fresh-state runs avoid Cargo artifact reuse across source mutations.
No live adapter, callback latency/RSS budget, daemon, desktop notification,
native approval submission, deployment or M2 feature is claimed qualified.

Further integrated review reproduced two cross-harness receipt bypasses: exact
lookup before immutable source-scope validation, and confirmation with a receipt
from another harness. Both checks now bind the source harness while allowing
exact historical generation replay. The private synthetic regression helper
remains under `cfg(test)`; the production eligibility registry remains empty.
A separate observed diagnostics RED omitted known volatile queue loss. The
corrected optional immutable job snapshot keeps this lower bound and unknown gap
separate from durable events and discarded artifacts; unavailable data is null.

[Final independent domain/storage review](independent-domain-storage/review-final.json)
passed the corrected 50-file scope, including retirement/migration. Its five
storage mutations failed as expected; all restored sources recompiled and passed.
[Independent diagnostics review](independent-diagnostics/review.json) passed all
five public tests on the immutable optional job snapshot implementation.
[Final independent coordinator review](independent-coordinator/review-final.json)
passed at `364f7c8d467594b3a7d5d79ea816d7e81b5903ef`, including 17 coordinator
cases, five reconciliation cases, ingestion and inventory. Seven causal removals
failed and restored runs passed. Its extra actual lost-reply/queued-observation
fixture proves unchanged Apply0, durable count0 and separate volatile count1;
it does not treat those units as distinct lost events.

One subsequent full 110-unit execution had 108 PASS and two intermittent
`AlreadyOwned` failures during recovery fixture reopen. Despite its directory
name, `coordinator/m1-10-coordinator-confirmation-green` records that failed run,
not a passing full suite. The subsequent independent diagnosis and test-only correction are linked below;
required final execution remains a separate qualification gate.

[Independent verifier-policy review](independent-verifier-policy/review.json)
passed 22 applicable regressions and checked the actual diff from qualified main.
Docker confinement, workflow, paired qualification, strict source/log validation
and all three native methods remain intact. The added fixed filesystem is private
and bounded; only Docker explicitly adds noexec on that mount. Its review is
separate from actual final hosted Docker and dedicated native execution.

[Independent lock-reopen diagnosis](independent-lock-reopen/review-final.json)
records a deterministic kernel fork/CLOEXEC probe and an actual ArtifactStore
stress probe: 21 transient refusals, no parent lock FD, and an inherited child FD.
The baseline nondeterminism did not recur in 20 fresh 111-test runs; that limit is
reported honestly. The correction changes only three explicit-drop reopen sites
and adds a guard against retained local ownership. Removing the guard produces
an actual causal failure. All 20 corrected full parallel runs passed 112 tests;
public recovery, strict Clippy and formatting also passed. Production lock behavior
and test parallelism are unchanged. The earlier failed run remains preserved.

## Final candidate qualification

The required final combined, dedicated native, actual hosted Docker and paired
records must bind the same selected source bytes of the published full commit.
Only trusted actual reports count; component passes and review verdicts do not
substitute for absent execution. The PR will identify the exact head, hosted run,
artifact and actual paired result before owner approval is requested.

[Final local combined](final-combined/report.json) passes all 34 required checks;
[dedicated native](final-native/report.json) passes all five, including every
preserved native integration method. No required skips; only the three declared
optional live/future probes skipped. Both actual 289-file selected-source
manifests match source commit `3bcb9f0f65889d665ad793ea7a942ad281a52086`.
Evidence-only publication commits can change the repository SHA while preserving
those selected bytes; the actual hosted/native pairing binds the published full
revision. A single execution still reports qualification_complete=false.
The PR's final evidence comment supplies hosted run/artifact and actual pairing.
