Independent read-only coordinator review started at source commit 3e68e80506e76dfff7b061ab64bde265b84b319c. Production code was never edited in the root checkout by this reviewer. Initial source hashes are in state.json. Recovery and diagnostics are excluded from this reviewer's independent source verdict because the reviewer authored them. Private synthetic recovery mappings are fixture setup only; the production mapping registry remains empty.

Actual findings: exact durable lookup preceded immutable source-harness comparison, allowing a byte-identical Codex synthetic envelope to obtain a receipt for a Hermes producer. Public confirmation also omitted source-harness identity, allowing an existing Hermes receipt to remove a Codex synthetic artifact. Both paths were reproduced with real private SQLite/artifact stores; original receipts are harness-scope-red.json and confirmation-harness-red.json. The first independent-initial.json also contains one fixture-setup failure (duplicate artifact identity correctly rejected by the recovery gate); the corrected independent fixture uses a separate private store, and harness-scope-red.json isolates the genuine source-scope failure.

Independent causal executions, followed by actual recompilation and restored success: (1) prematurely issue a receipt after context instead of Apply commit; (2) accept a Conflict receipt as exact; (3) remove the installed-generation comparison before invalidation, observing an actual queued new-generation event discarded; (4) omit expected/observed runtime identity equality, admitting reused PID/start evidence. Each receipt identifies exact mutation bytes and command. Product executions used the unchanged native bwrap policy with private prepared tools, no network, no host state/config/harness database, and a fresh private target. Restoring source used fresh mtimes and the logs explicitly contain Compiling harbormaster.

Maintainability disposition: registry.rs355 keeps one authority/queue/receipt owner; the new installation and generation invalidation seams share these invariants directly, and a metric-only split would hide that relationship. Event protocol306 grows only by a closed pure turn-id projection beside its enum. Checks.plan58 grows by four explicit mandatory targets, preserving ordered preflights and auditable native/portable inventories without additional branching. Accept the cohesive actual coordinator crash52 and missing-hook51 test lifecycles. Existing control.rs303 is unchanged. Recovery.write_stage52 is self-authored and excluded; the separate reviewer accepted its cohesive file lifecycle. Storage-specific test triggers are handled by the independent storage reviewer.

Final corrected-source verdict pending corrected root checkpoint and rerun. No claim of Docker/native paired qualification, adapter qualification, deployment, notification delivery, or M2 is made here.
